/** @type {import('next').NextConfig} */
const nextConfig = {
  output: 'export',
  basePath: '/Dhawi_Dahn/agency',
  assetPrefix: '/Dhawi_Dahn/agency',
  trailingSlash: true,
  images: {
    unoptimized: true,
  },
};

export default nextConfig;
