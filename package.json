{
  "name": "dhawi-dahn",
  "version": "1.0.0",
  "private": true,
  "description": "ضاوي دهن العود — معمل العود الخاص",
  "scripts": {
    "build": "esbuild app.jsx --bundle --minify --format=iife --target=es2018 --loader:.jsx=jsx --jsx=automatic --outfile=app.js --define:process.env.NODE_ENV=\\\"production\\\"",
    "watch": "npm run build -- --watch",
    "check": "node scripts/check-build.mjs"
  },
  "dependencies": {
    "react": "18.3.1",
    "react-dom": "18.3.1"
  },
  "devDependencies": {
    "esbuild": "0.28.2"
  }
}
